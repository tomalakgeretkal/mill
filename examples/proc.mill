#!/usr/bin/env runmill
#(
A program which demonstrates how to define and call procedures.
)

use std::io;

proc show(message: String) {
    io::writeln(message);
}

proc hi() {
    show("Hello, world!");
}

proc bye() {
    show("Goodbye, world!");
}

MAIN {
    hi();
    bye();
}
